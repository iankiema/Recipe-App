<div class="container">
  <h1 class="my-4">New Recipe Food</h1>

  <%= render 'form', recipe: @recipe, recipe_food: @recipe_food %>

  <%= link_to 'Back', recipe_path(@recipe), class: 'btn btn-secondary' %>
</div>